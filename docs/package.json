{
  "name": "queenbee-doc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:worker && npm run build:user",
    "build:worker": "redoc-cli bundle api/worker.yaml && npm run cp:en && npm run cp:zh",
    "build:user": "redoc-cli bundle api/user.yaml && npm run cp:en && npm run cp:zh",
    "cp:en": "cp redoc-static.html content/en/api/worker/index.html",
    "cp:zh": "cp redoc-static.html content/zh/api/worker/index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^9.8.0",
    "postcss-cli": "^7.1.1",
    "redoc-cli": "^0.9.8"
  }
}
